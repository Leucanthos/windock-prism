// ============================================================
// Version — single source of truth for build version
// ============================================================
static class VersionInfo
{
    public const string Number = "0.1.0";
    public const string Name = "WinDock";
    public const string FullName = Name + " v" + Number;
}
